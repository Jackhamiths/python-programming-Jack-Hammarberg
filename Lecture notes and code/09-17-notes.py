# tjena

import matplotlib.pyplot as plt
import numpy as np

x = list(range(10)) # creates a list of intagers
k = 2
m = 2
y = [k*x*m for x in x]


def draw_circle(radius = 1, center = (0,0)): 
    x = np.linspace(0, 2*np.pi)



